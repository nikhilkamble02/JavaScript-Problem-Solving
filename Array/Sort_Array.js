const arr = [5, 1, 3, 20, 40];

//ascending order
const ascendingArray = arr.sort((a,b) => a-b);
console.log(ascendingArray)

// descending order
const descendingArray = arr.sort((a,b) => b-a);
console.log(descendingArray)

